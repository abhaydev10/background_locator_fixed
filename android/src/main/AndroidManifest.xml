<!-- <manifest package="yukams.app.background_locator_2"></manifest> -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Remove package attribute since namespace is now in build.gradle -->
    
    <!-- Keep your existing permissions and other manifest content -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    
    <application>
        <!-- Your existing service declarations -->
    </application>
</manifest>
